import {
  colors,
  fonts,
  fontSizes,
  fontWeights,
  letterSpacings,
  lineHeights,
  radii,
  shadows,
  sizes,
  space,
  zIndices,
} from '../tokens'

const Theme = {
  colors,
  space,
  sizes,
  fonts,
  fontSizes,
  shadows,
  lineHeights,
  fontWeights,
  letterSpacings,
  radii,
  zIndices,
}

export default Theme
